language: node_js
node_js: 
  - "8"

# S: Build Lifecycle
install:
  - npm install


#before_script:
 # - npm install -g gulp

script:
  - hexo g

after_script:
  - cd ./public
  - git init
  - git config user.name "qibotian"
  - git config user.email "hashcoder@163.com"
  - git add .
  - git commit -m "Update docs"
  - git push --force --quiet "https://${git-token}@${QBT_REF}" master:master
# E: Build LifeCycle

branches:
  only:
    - blog-source
env:
 global:
   - QBT_REF: qibotian/qibotian.github.io.git